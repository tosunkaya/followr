<%= form_for(@twitter_follow) do |f| %>
  <% if @twitter_follow.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twitter_follow.errors.count, "error") %> prohibited this twitter_follow from being saved:</h2>

      <ul>
      <% @twitter_follow.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
