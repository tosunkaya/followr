<%= form_for @twitter_follow_preference do |f| %>
  <% if @twitter_follow_preference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@twitter_follow_preference.errors.count, "error") %> prohibited this twitter_follow_preference from being saved:</h2>

      <ul>
      <% @twitter_follow_preference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <label for='unfollow_after'>Unfollow user after how many days?</label><br>
  <%= f.text_field :unfollow_after %><br>

  <%= f.label :hashtags, value: "Hashtags to follow users from (comma separated)" %><br>
  #<%= f.text_field :hashtags, placeholder: "Ex: javascript,catgifs,shipfam" %><br>
  <br>

  <%= f.label :mass_follow, value: "Mass follow users?" %>
  <%= f.check_box :mass_follow %><br>

  <%= f.label :mass_unfollow, value: "Mass unfollow users?" %>
  <%= f.check_box :mass_unfollow %><br>

  <div class="actions">
    <%= f.submit 'Submit' %>
  </div>
<% end %>